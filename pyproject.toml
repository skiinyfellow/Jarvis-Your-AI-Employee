[project]
name = "jarvis-ai-employee"
version = "0.1.0"
description = "Jarvis AI Employee Platform"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "uvicorn",
    "python-dotenv"
]

[build-system]
requires = [
    "setuptools"
]
build-backend = "setuptools.build_meta"