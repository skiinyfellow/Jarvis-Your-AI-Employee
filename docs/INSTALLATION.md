# Jarvis Installation Guide

## Requirements

- Python 3.11+
- Docker
- Git

## Installation

Clone repository:

```
git clone repository-url
```

Install dependencies:

```
pip install -r requirements.txt
```

Start services:

```
docker compose up -d
```